<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- For development: Allow HTTP traffic to local XAMPP server -->
    <!-- WARNING: For production, use HTTPS only! -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>

